

import java.io.IOException;
import java.io.PrintWriter;
import java.sql.Connection;
import java.sql.Driver;
import java.sql.DriverManager;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.mysql.jdbc.PreparedStatement;

@WebServlet("/Insert_demo")
public class Insert_demo extends HttpServlet {
	private static final long serialVersionUID = 1L;

	
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException 
	{
		response.setContentType("text/html");
		PrintWriter pw = response.getWriter();
		String Qry;
		PreparedStatement pstmt = null;
		String  URL, Username, Password;
		URL = "jdbc:mysql://localhost:3306/demo";
		Username = "root";
		Password = "";
		
		String name = request.getParameter("txtName");
		String gender = request.getParameter("txtGender");
		String bdate = request.getParameter("txtBdate");
		String add = request.getParameter("txtAdd");
		String city = request.getParameter("txtCity");
		try {
			Connection cn = null;
			Class.forName("com.mysql.jdbc.Driver");
			pw.write("Class Loaded...");
			DriverManager.getConnection(URL, Username, Password);
			pw.write("Connection Success...");
			Qry = "Insert into tbl_demo (?, ?, ?, ?, ?) values (?, ?, ?, ?, ?)";
			pw.println(Qry);
			pstmt = (PreparedStatement) cn.prepareStatement(Qry);
			pw.println(cn.prepareStatement(Qry));
			pstmt.setString(1, name); 
			pw.println(name);
			pstmt.setString(2, gender);
			pw.println(gender);
			pstmt.setString(3, bdate); 
			pw.println(bdate);
			pstmt.setString(4, add);
			pw.println(add);
			pstmt.setString(5, city); 
			pw.println(city);
			pstmt.close();
			cn.close();
			System.out.println("record inserted");
		} catch (Exception e) {
			e.printStackTrace();
		}
	}

}
